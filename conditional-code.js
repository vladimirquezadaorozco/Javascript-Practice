const myName = 'Max'

if (myName === 'Max') {
    for (letter of myName) {
        console.log(letter)
    }
}

let isLoggedIn = true;

if (isLoggedIn) {
    console.log('User is logged in')
}

const entredUserName = ''

if (entredUserName) {
    console.log('input is valid')
}